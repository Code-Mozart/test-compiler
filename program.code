// for now:
// all procedures are void and may only take one argument

proc main() {
	print_sum_n_times(5, 8, 4);
}

proc print_sum_n_times(var num1, var num2, var times) {
	var i = 0;
	while (i < times) {
		print(num1 + num2);
		i = i + 1;
	}
}
