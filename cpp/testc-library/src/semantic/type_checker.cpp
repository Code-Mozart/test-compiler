#include "type_checker.h"

namespace testc {

    Result resolve_references(const List<Ref<AST_Procedure>>& procedures, Ref<Symbol_Table> symbols) {
        NOT_IMPL();
    }

    Result type_check(Ref<const AST_Node> node) {
        NOT_IMPL();
    }

}
